name: PRview

on:
  pull_request:
    types: [opened, synchronize, reopened, closed]

permissions:
  contents: write
  pull-requests: write

jobs:
  preview:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # --- デプロイ開始コメント ---
      - name: Comment start
        if: github.event.action != 'closed'
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request.number;
            github.rest.issues.createComment({
              ...context.repo,
              issue_number: pr,
              body: `Deploy started for PR #${pr}...`
            });

      # --- ファイル準備（修正済み） ---
      - name: Prepare files
        if: github.event.action != 'closed'
        run: |
          mkdir out
          rsync -av --exclude=out --exclude=.git --exclude=.github ./ out/

      # --- デプロイ ---
      - name: Deploy to Pages
        if: github.event.action != 'closed'
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./out
          destination_dir: pr-${{ github.event.number }}

      # --- 完了コメント ---
      - name: Comment preview URL
        if: github.event.action != 'closed'
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request.number;
            const url = `https://${context.repo.owner}.github.io/${context.repo.repo}/pr-${pr}/`;

            github.rest.issues.createComment({
              ...context.repo,
              issue_number: pr,
              body: `Preview ready:\n${url}`
            });

      # --- PRクローズ時 cleanup ---
      - name: Checkout gh-pages
        if: github.event.action == 'closed'
        uses: actions/checkout@v4
        with:
          ref: gh-pages

      - name: Remove preview
        if: github.event.action == 'closed'
        run: |
          rm -rf pr-${{ github.event.number }}

      - name: Commit & push
        if: github.event.action == 'closed'
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .
          git commit -m "Remove preview for PR #${{ github.event.number }}" || echo "No changes"
          git push
